#!/bin/sh
#Generated by JetBrains Toolbox 1.20.7940 at 2021-03-21T14:46:23.034080

declare -a ideargs=()
declare -- wait=""

for o in "$@"; do
  if [[ "$o" = "--wait" || "$o" = "-w" ]]; then
    wait="-W"
    o="--wait"
  fi
  if [[ "$o" =~ " " ]]; then
    ideargs+=("\"$o\"")
  else
    ideargs+=("$o")
  fi
done

open -na "/Users/jarod/Library/Application Support/JetBrains/Toolbox/apps/Goland/ch-0/202.5958.19/GoLand 2020.2 EAP.app/Contents/MacOS/goland" $wait --args "${ideargs[@]}"