#!/bin/sh
#Generated by JetBrains Toolbox 1.20.7940 at 2021-02-22T20:13:43.201209

declare -a ideargs=()
declare -- wait=""

for o in "$@"; do
  if [[ "$o" = "--wait" || "$o" = "-w" ]]; then
    wait="-W"
    o="--wait"
  fi
  if [[ "$o" =~ " " ]]; then
    ideargs+=("\"$o\"")
  else
    ideargs+=("$o")
  fi
done

open -na "/Users/jarod/Library/Application Support/JetBrains/Toolbox/apps/RubyMine/ch-0/201.7846.78/RubyMine.app/Contents/MacOS/rubymine" $wait --args "${ideargs[@]}"